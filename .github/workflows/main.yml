name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]


jobs:
  build:
    runs-on: ubuntu-lastest

    steps:
    - uses: actions/checkout@v2
    - name: Run Hello World!
      run: echo Hello, World!
